# =============================================================================
# Authentication Service — Local Development Overrides
# =============================================================================
services:
  auth_app:
    dns: []
    volumes:
      - .:/app
      - /app/.venv

  auth_celery:
    volumes:
      - .:/app
      - /app/.venv

  auth_celery_beat:
    volumes:
      - .:/app
      - /app/.venv
